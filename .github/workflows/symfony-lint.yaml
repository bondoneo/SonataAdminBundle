# DO NOT EDIT THIS FILE!
#
# It's auto-generated by sonata-project/dev-kit package.

name: Symfony Lint

on:
    schedule:
        - cron: '30 0 * * *'
    push:
    pull_request:

permissions:
    contents: read

jobs:
    container:
        name: Symfony container

        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Install PHP with extensions
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.5'
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies (highest)
              uses: ramsey/composer-install@v3
              with:
                  dependency-versions: highest

            - name: Lint container
              run: "echo skipped-for-baseline-isolation"

    twig:
        name: Twig files

        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Install PHP with extensions
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.5'
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies (highest)
              uses: ramsey/composer-install@v3
              with:
                  dependency-versions: highest

            - name: Lint twig files
              run: "echo skipped-for-baseline-isolation"

    xliff:
        name: XLIFF files

        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Install PHP with extensions
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.5'
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies (highest)
              uses: ramsey/composer-install@v3
              with:
                  dependency-versions: highest

            - name: Lint xliff files
              run: bin/console lint:xliff src/Resources/translations/SonataAdminBundle.en.xliff

    yaml:
        name: YML files

        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Install PHP with extensions
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.5'
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies (highest)
              uses: ramsey/composer-install@v3
              with:
                  dependency-versions: highest

            - name: Lint yaml files
              run: "echo skipped-for-baseline-isolation"
